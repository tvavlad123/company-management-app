export class Authority{
    public id: number;
    public authorityType : string;

    constructor(id: number, authorityType: string){
        this.id=id;
        this.authorityType=authorityType;
    }
}
